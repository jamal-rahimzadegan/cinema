import { makeUuid } from "utils";

export default function mapCinemaListDtoToCinemaListData(cinemaList: CinemaItem[]) {
	// let mappedCinemaList = [...cinemaList];
	//
	// cinemaList.forEach((cinema, i) => {
	// 	mappedCinemaList[i].uid = makeUuid();
	// });
	//
	// return mappedCinemaList;

	return cinemaList;
}
